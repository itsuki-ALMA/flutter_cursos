import 'package:flutter/material.dart';

class AppColor {
  static const Color primary = Color(0xFF627E97);     // azul principal
  static const Color accent = Color(0xFF95A5B8);      // tom secundário mais claro
  static const Color background = Color(0xFFF5F7FA);  // fundo geral, cinza muito claro puxado para azul
  static const Color surface = Color(0xFFFFFFFF);     // para superfícies internas (cards, etc), ainda branco
  static const Color inputFill = Color(0xFFE6EBF0);   // fundo dos inputs, cinza claro azulado
  static const Color border = Color(0xFF9BADBE);      // bordas dos inputs, cinza azulado
  static const Color divider = Color(0xFFCAD5DF);     // divisores
  static const Color text = Color(0xFF2E3E50);        // texto principal, cinza escuro
  static const Color hint = Color(0xFF7A8C9F);        // texto de dica, cinza médio
  static const Color onPrimary = Color(0xFFFFFFFF);   // texto sobre botão primary (branco)
  static const Color error = Color(0xFFB00020);       // vermelho padrão para erros
}
